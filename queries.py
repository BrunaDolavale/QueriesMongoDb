##Modelo que retorna 1:
  ##print(collection_docentes.find_one({ 'pessoa.nacionalidade': { "$not": { "$eq": 'Brasil' } } }))
##****************************************************************************************************##
